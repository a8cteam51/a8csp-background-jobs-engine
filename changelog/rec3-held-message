Significance: patch
Type: changed

The `overlap_held` message names the run that holds the overlap lock and says to wait for the lock's release, rather than saying that run is still running. A run whose process died, or whose run-row or lock storage failed, after it was marked finished and before its lock was released keeps that lock until the lock goes stale or maintenance replays the run, so it may already be finished.
