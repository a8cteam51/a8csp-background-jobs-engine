Significance: patch
Type: changed

`RunContextInterface` joins `ChunkedRunContextInterface` as an engine-implemented interface: consumers must not implement it, and minor versions may add methods to it. A test that calls `handle()` or `generate_queue()` directly passes a `RunContext` instead of its own implementation.
