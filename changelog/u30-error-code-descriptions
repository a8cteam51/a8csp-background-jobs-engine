Significance: patch
Type: fixed

Three `ErrorCode` case descriptions state what produces them. `overlap_held` is reported for a held overlap lock and never for a schedule's occurrence decision; `admission_conflict` means a lost admission race whatever the number of attempts behind it; and `unsupported_operation` names a state the engine cannot serve rather than a backend or version limitation. The cases and their values are unchanged.
