Significance: patch
Type: fixed

A cleanup intent skipped because a read fails emits a warning naming the failed phase and the later occurrence or sweep that can retry cleanup. A sweep whose cursor update is unconfirmed emits a warning naming the attempted cursor and outcome and explaining that the next sweep resumes from the durable cursor state.
