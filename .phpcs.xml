<?xml version="1.0"?>
<ruleset name="a8csp-background-tasks" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="vendor/squizlabs/php_codesniffer/phpcs.xsd">
	<description>Custom ruleset for the A8CSP plugin.</description>

	<!-- Extend the A8CSP ruleset. -->
	<rule ref="./vendor/a8cteam51/team51-configs/quality-tools/phpcs.dist.xml"/>

	<!-- Check that the proper text domain(s) is used everywhere. -->
	<rule ref="WordPress.WP.I18n">
		<properties>
			<property name="text_domain" type="array">
				<element value="a8csp-background-tasks"/>
			</property>
		</properties>
	</rule>

	<!-- Check that the proper prefix is used everywhere. -->
	<rule ref="WordPress.NamingConventions.PrefixAllGlobals">
		<properties>
			<property name="prefixes" type="array">
				<element value="a8csp"/>
				<element value="a8csp_bgt"/>
				<element value="a8csp_background_tasks"/>
				<element value="A8C\SpecialProjects\BackgroundTasks"/>
			</property>
		</properties>
	</rule>

	<!-- Allow additional word delimiters for hook names. -->
	<rule ref="WordPress.NamingConventions.ValidHookName">
		<properties>
			<property name="additionalWordDelimiters" value="/"/>
		</properties>
	</rule>
</ruleset>
